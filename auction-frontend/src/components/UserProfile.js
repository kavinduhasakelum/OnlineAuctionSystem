import React from 'react';
import { Container, Alert } from 'react-bootstrap';

const UserProfile = () => {
  return (
    <Container>
      <h1>User Profile</h1>
      <Alert variant="info">
        User profile functionality will be implemented with backend integration
      </Alert>
    </Container>
  );
};

export default UserProfile;
